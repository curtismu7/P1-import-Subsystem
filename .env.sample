# PingOne Import Tool - Sample Environment Configuration
# Copy this file to .env and update with your actual values

# PingOne API Configuration
PINGONE_ENVIRONMENT_ID=your-pingone-environment-id-here
PINGONE_CLIENT_ID=your-pingone-client-id-here
PINGONE_CLIENT_SECRET=your-pingone-client-secret-here
PINGONE_REGION=NorthAmerica

# Server Configuration
PORT=4000
NODE_ENV=development

# Authentication Subsystem
AUTH_SUBSYSTEM_ENCRYPTION_KEY=your-32-character-encryption-key-here

# Optional: Auto-kill processes using the port (use with caution)
AUTO_KILL_PORT=false

# Optional: Skip duplicate checking (for testing)
SKIP_DUPLICATE_CHECK=false

# Optional: Application version (fallback)
APP_VERSION=6.1.0

# Instructions:
# 1. Replace 'your-pingone-environment-id-here' with your actual PingOne Environment ID
# 2. Replace 'your-pingone-client-id-here' with your actual PingOne Client ID
# 3. Replace 'your-pingone-client-secret-here' with your actual PingOne Client Secret
# 4. Set PINGONE_REGION to your region: NorthAmerica, Europe, AsiaPacific, or Canada
# 5. Generate a secure 32-character encryption key for AUTH_SUBSYSTEM_ENCRYPTION_KEY
# 6. Adjust PORT if needed (default: 4000)
# 7. Set NODE_ENV to 'production' for production deployments

# Security Notes:
# - Never commit this file to version control
# - Keep your client secret secure and rotate it regularly
# - Use a strong, unique encryption key for the auth subsystem
# - Ensure proper file permissions (600) on this file in production
